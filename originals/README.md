Drop raw client photos here under whatever names they arrive with.
Don't rename or resize — the web-sized, correctly-named versions get
written to `img/` and these stay as the source of truth.

See IMAGE-SPEC.md in the repo root for the filenames the page expects.
